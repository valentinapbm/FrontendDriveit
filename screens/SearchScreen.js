import React from 'react';
import {SafeAreaView, Text, View} from 'react-native';
const SearchScreen = () => {
    return (
        <View
        style={{flex: 1, justifyContent: 'center', alignItems: 'center'}}>
        <Text>SearchScreen user</Text>
        </View>
    );
};

export default SearchScreen;